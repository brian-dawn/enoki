
inc = +(1)
result = inc(4)
println(result)

# Lists are immutable.
some-list = [1, 2, 3]
another-list = map(inc, push(some-list, 4))
println(some-list)
println(another-list)


println(reduce(+, another-list))

# We can create seqs with ranges.
println(map(inc, 1..4))

# We can also make infinite sequences.
println(map(inc, take(10, 1..)))
